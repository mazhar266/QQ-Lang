/target

# Fuzzing — build output and generated corpora, not source
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
/fuzz/coverage
/fuzz/Cargo.lock

# Vector indexes — generated from committed text by scripts/build-vectors.py,
# and only needed with the `vector` feature. Regenerable, so not carried.
/sources/vectors

# Dart
.dart_tool/
.packages
